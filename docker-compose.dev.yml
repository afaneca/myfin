name: myfin_fe

services: 
  web:
    container_name: myfin_frontend
    image: myfin_frontend:dev
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
      - ./.env:/app/.env
